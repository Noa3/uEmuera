# Local game descriptors

Register only metadata here. Copyrighted game contents stay outside the
repository. A descriptor may include a developer-local path through an
untracked override file, an optional version/hash, runtime kind and explicit
scenario names. A title-screen boot is not compatibility evidence.
