# Fixtures and Expected output

`Fixtures/` contains developer-supplied deterministic `.erb` inputs.
`Expected/` contains only captures produced by `Reference/run_fixture.py`.
Reference outputs must come from an actual EM+EE process; hand-written
Expected files are not valid evidence.
